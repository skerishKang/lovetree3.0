import { describe, expect, it } from "vitest";

describe.skip("CommunityPage public tree links (diagnostic isolation)", () => {
  it("is restored after the failing suite is isolated", () => {
    expect(true).toBe(true);
  });
});
